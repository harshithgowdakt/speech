apiVersion: v2
name: asr-gateway
description: WebSocket ASR streaming gateway that relays client audio to a gRPC ASR inference server and streams transcripts back in real time.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Version of the app (container image) this chart deploys by default.
appVersion: "0.1.0"
keywords:
  - asr
  - speech-to-text
  - websocket
  - grpc
  - streaming
home: https://github.com/harshithgowdakt/speech
sources:
  - https://github.com/harshithgowdakt/speech
maintainers:
  - name: harshithgowdakt
